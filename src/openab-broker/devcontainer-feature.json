{
    "name": "OpenAB Broker",
    "id": "openab-broker",
    "version": "1.1.0",
    "description": "Open Agent Broker (openab) — Discord bot bridging to ACP-enabled coding agents. Builds openab from source; agent CLIs must be provided by other features.",
    "options": {
        "version": {
            "type": "string",
            "default": "0.7.6",
            "description": "openab version to build. Use a semver (e.g. '0.7.6' → tag v0.7.6), 'latest' to build from the main branch, or any git ref (branch/tag/SHA)."
        },
        "repoUrl": {
            "type": "string",
            "default": "https://github.com/openabdev/openab.git",
            "description": "Git repository URL to clone openab from"
        },
        "defaultAgent": {
            "type": "string",
            "enum": ["kiro-cli", "claude", "codex", "gemini", "copilot"],
            "default": "copilot",
            "description": "Which agent to wire into the generated config.toml [agent] section"
        },
        "configPath": {
            "type": "string",
            "default": "/etc/openab/config.toml",
            "description": "Path where the generated config.toml is written"
        },
        "allowedChannels": {
            "type": "string",
            "default": "",
            "description": "Comma-separated Discord channel IDs allowed to talk to the broker (empty = placeholder)"
        },
        "allowedRoleIds": {
            "type": "string",
            "default": "",
            "description": "Comma-separated Discord role IDs allowed to talk to the broker (empty = none)"
        },
        "trustedBotIds": {
            "type": "string",
            "default": "",
            "description": "Comma-separated Discord bot user IDs whose messages the broker will trust/accept (empty = none)"
        },
        "allowedUsers": {
            "type": "string",
            "default": "",
            "description": "Comma-separated Discord user IDs allowed to talk to the broker (empty = none)"
        },
        "maxSessions": {
            "type": "string",
            "default": "10",
            "description": "pool.max_sessions value written into config.toml"
        },
        "autostart": {
            "type": "boolean",
            "default": false,
            "description": "Start openab automatically via supervisor. Requires DISCORD_BOT_TOKEN to be set in the container environment."
        }
    },
    "entrypoint": "/usr/local/bin/devdesk-openab-entrypoint",
    "installsAfter": [
        "ghcr.io/ricky1698/devcontainer-features/devdesk-base"
    ]
}
