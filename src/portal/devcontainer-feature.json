{
    "name": "Portal",
    "id": "portal",
    "version": "1.1.4",
    "description": "DevDesk Portal with nginx reverse proxy for web services on port 80",
    "options": {
        "services": {
            "type": "string",
            "default": "noVNC:6080:VNC web client:monitor,Code Server:8888:VS Code in browser:terminal",
            "description": "Comma-separated services in format name:port:description:icon"
        },
        "links": {
            "type": "string",
            "default": "",
            "description": "Comma-separated direct links in format name:port:description:icon (for services that don't work behind reverse proxy)"
        },
        "ttyd": {
            "type": "boolean",
            "default": true,
            "description": "Install and configure ttyd for in-browser terminal (auto-added as a service)"
        },
        "ttyd_port": {
            "type": "string",
            "default": "7681",
            "description": "Port for ttyd to listen on (localhost only)"
        }
    },
    "entrypoint": "/usr/local/bin/devdesk-portal-entrypoint",
    "installsAfter": [
        "ghcr.io/devcontainers/features/common-utils"
    ]
}
