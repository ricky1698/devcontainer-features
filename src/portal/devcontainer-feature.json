{
    "name": "Portal",
    "id": "portal",
    "version": "1.0.6",
    "description": "DevDesk Portal with nginx reverse proxy for web services on port 80",
    "options": {
        "services": {
            "type": "string",
            "default": "noVNC:6080:VNC web client:monitor,Code Server:8888:VS Code in browser:terminal",
            "description": "Comma-separated services in format name:port:description:icon"
        }
    },
    "entrypoint": "/usr/local/bin/devdesk-portal-entrypoint",
    "installsAfter": [
        "ghcr.io/devcontainers/features/common-utils"
    ]
}
