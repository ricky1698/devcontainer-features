{
    "name": "Portal",
    "id": "portal",
    "version": "1.0.3",
    "description": "DevDesk Portal with nginx reverse proxy for web services",
    "options": {
        "port": {
            "type": "string",
            "default": "8080",
            "description": "Port for the portal nginx server"
        },
        "services": {
            "type": "string",
            "default": "noVNC:6080:VNC web client:monitor,Code Server:8888:VS Code in browser:terminal",
            "description": "Comma-separated services in format name:port:description:icon"
        }
    },
    "installsAfter": [
        "ghcr.io/devcontainers/features/common-utils"
    ]
}
