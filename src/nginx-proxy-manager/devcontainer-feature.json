{
    "name": "Nginx Proxy Manager",
    "id": "nginx-proxy-manager",
    "version": "1.0.0",
    "description": "Nginx Proxy Manager with HTTPS support. Admin UI on port 81, HTTP on port 80, HTTPS on port 443. Each service gets a {name}.localhost subdomain.",
    "options": {
        "services": {
            "type": "string",
            "default": "noVNC:6080:VNC web client:monitor,Code Server:8888:VS Code in browser:terminal",
            "description": "Comma-separated services in format name:port:description:icon. Each service gets a {name}.localhost subdomain."
        },
        "adminEmail": {
            "type": "string",
            "default": "admin@example.com",
            "description": "NPM admin email address"
        },
        "adminPassword": {
            "type": "string",
            "default": "changeme",
            "description": "NPM admin password"
        },
        "httpsOnly": {
            "type": "boolean",
            "default": false,
            "description": "Force HTTP to HTTPS redirect on all proxy hosts"
        }
    },
    "entrypoint": "/usr/local/bin/devdesk-npm-entrypoint",
    "installsAfter": [
        "ghcr.io/devcontainers/features/common-utils"
    ]
}
